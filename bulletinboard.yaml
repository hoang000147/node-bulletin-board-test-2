apiVersion: apps/v1
kind: Deployment
metadata:
  name: bb-demo
  namespace: bulletin-board-test
spec:
  replicas: 1
  selector:
    matchLabels:
      bb: web
  template:
    metadata:
      labels:
        bb: web
    spec:
      containers:
      - name: bb-site
        image: hoang000147/bulletinboard:tagVersion
---
apiVersion: v1
kind: Service
metadata:
  name: bb-entrypoint
  namespace: bulletin-board-test
spec:
  type: NodePort
  selector:
    bb: web
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30015
